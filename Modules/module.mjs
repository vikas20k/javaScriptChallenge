export const addfun = (num, num1) => {
    return num + num1;
}

export const subfun = (num, num1) => {
    return num - num1;
}

export const mulfun = (num, num1) => {
    return num * num1;
}

export const divfun = (num, num1) => {
    return num / num1;
}

